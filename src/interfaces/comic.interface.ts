export interface ComicInterface {
    _id: string
    id?: string
    title: string
    description: string
    publication: Date
    thumbnail: string
    pageCount: number
    createdAt: Date
    updatedAt: Date
}